Suggestions:
1. Install bpftrace and write a 10 line script tracing something concrete e.g. syscall latency
2. Deploy Parca or Pyroscope to a local kind/minikube cluster, profile one of own toy services, read the flamegraph
3. Compare Beyla/OBI's zero-code profiling output against own manually-instrumented version -- this one bridges observability and performance directly
