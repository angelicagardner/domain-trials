Suggestions:
1. Instrument a tiny agent loop (call an LLM API, maybe one tool call) with the OTel GenAI conventions manually to view the trace
2. Build a minimal eval harness - fixed prompts, simple scoring, logged as trace data
3. Run same toy agent through Languse or Phoenix, compare to raw OTel version
